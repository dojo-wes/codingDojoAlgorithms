// put integers between 1 and 255 inclusive in an array

// initialize an empty array
// for loop
  // start at 1
  // while i < 256
  // increment by 2
// .push number into array

var arr = [];
for(var i = 1; i < 256; i += 2) {
  arr.push(i);
}
// console.log(arr);
console.log(arr[arr.length -1]);